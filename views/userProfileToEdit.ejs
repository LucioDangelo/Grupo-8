<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar perfil de usuario</title>
    <link rel="stylesheet" href="/css/styleuserprofile.css">
</head>
<body>  
    <!-- Navbar -->
    <%- include('./partials/headerNavbar') %>
    
    <!-- <section class="user-profile">
        <!-- <% if (locals.errors) { %>
            <% for ( let oneError in errors ) { %>
                <p class="errores"><%= errors[oneError].msg %></p>
              <% } %>
              <%} %> -->
        <div class="avatar-profile">
          <img class="profile-avatar" src="/img/avatars/<%= locals.userLogged.avatar || 'default-avatar.png' %>"  alt="avatar" width="100%" height="100%">    
        </div>
        <div class="container-0">
          <div class="container-1">
                <h2 class="h2-prof">Bienvenido <div class="user-text"><%= locals.userLogged.user_alias %></div></h2>
          </div>
          <div class="container-2">
                <form class="container-form" action="/users/profileToEdit/<%=locals.userLogged.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <h2 class="data-h2">Datos del perfil</h2>
                    <div class="data-user">
                        <label class="data-user-lab">Nombre</label>
                        <div class="data-user-div">
                            <input class="input-form" type="text" name="name" value="<%= locals.userLogged.first_name %>" required>
                            <% if (typeof errors != 'undefined' && typeof errors.name != 'undefined') { %>
                                <span style="color: red"><%= errors.name.msg %></span> 
                              <% } %>
                        </div>
                    </div>
                    <div class="data-user">
                        <label class="data-user-lab">Apellido</label>
                        <div class="data-user-div">
                            <input class="input-form" type="text" name="lastName" value="<%= locals.userLogged.last_name %>" required>
                        </div>
                    </div>
                    <div class="data-user">
                        <label class="data-user-lab">Usuario</label>
                        <div class="data-user-div">
                            <input class="input-form" type="text" name="userAlias" value="<%= locals.userLogged.user_alias %>" required>
                        </div>
                    </div>
                    <div class="data-user">
                        <label class="data-user-lab">E-Mail</label>
                        <div class="data-user-div">
                            <input class="input-form" type="email" name="eMail" value="<%= locals.userLogged.email %>" disabled selected required>
                        </div>
                    </div>
                    <input class="button" id="submit" type="submit" value="Guardar cambios">
                </form>
            </div>
        </div>
        
      
      
      </section> -->

    <!-- <form class="container-form" action="/users/profileToEdit/<%=locals.userLogged.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <h2 class="h2-prof-edit">Datos a modificar</h2>
        <label>Nombre</label>
        <input class="input-form" type="text" name="name" value="<%= locals.userLogged.first_name %>" required>
        <label>Apellido</label>
        <input class="input-form" type="text" name="lastName" value="<%= locals.userLogged.last_name %>" required>
        <label>Usuario</label>
        <input class="input-form" type="text" name="userAlias" value="<%= locals.userLogged.user_alias %>" required>
        <label>E-Mail</label>
        <input class="input-form" type="email" name="eMail" value="<%= locals.userLogged.email %>" disabled selected required>
        
        <input class="button-for" id="submit" type="submit" value="Guardar cambios">
    </form>-->
    
    <!-- Footer --> 
    <%- include('./partials/footer') %>
     
</body>
</html>

